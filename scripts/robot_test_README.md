# Run Robot Framework API tests for ESP8266 D1 Control
# Usage:
#   docker build -t esp8266-robot-tests .
#   docker run --rm -e DEVICE_IP=192.168.1.107 esp8266-robot-tests

# To override the device IP, set the DEVICE_IP environment variable.
# Results will be shown in the container output.
